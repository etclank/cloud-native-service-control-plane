# Local review fixture for the H8.4B runtime foundation. The production
# values.yaml keeps both dependencies disabled, and Argo CD does not reference
# this file. H8.4C must replace the inert zero-target Prometheus configuration
# before any deployment is proposed.
prometheus:
  enabled: true

kube-state-metrics:
  enabled: true
